#include<stdio.h>
int main()
{
    int num=122,rem,originalnum,reversed=0;
    originalnum=num;
    while(num!=0)
    {
        rem=num%10;
        reversed=reversed*10+rem;
        num=num/10;
    }
    if(originalnum==reversed)
    printf("%d is a palindrome",originalnum);
    else
    printf("%d is not a palindrome",originalnum);
}
