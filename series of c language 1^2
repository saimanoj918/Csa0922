#include<stdio.h>
int main()
{
    int num=10,i,sum=1;
    printf("%d^2",sum);
    for(i=2;i<=num;i++)
    {
        printf(" +%d^2",i);
        sum+=i*i;
    }
            printf("=%d",sum);
    return 0;
}
